<%= form_for(@mm_team) do |f| %>
  <% if @mm_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mm_team.errors.count, "error") %> prohibited this mm_team from being saved:</h2>
    </div>
  <% end %>

	<div class="form-group">
	    Team Name<br />
	    <%= f.text_field :name, class: 'form-control' %>
	</div>
	<div class="form-group">
		<%= f.label :user %><br />
		<%= select(:mm_team, :user_id, options_from_collection_for_select(User.all, :id, :email)) %><br/>
	</div>

  <div class="actions padding-md text-center">
    <%= f.submit 'Submit', class: 'btn btn-success' %>
  </div>
<% end %>
